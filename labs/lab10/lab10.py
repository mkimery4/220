"""
Name: Margaret Kimery
<()>.py
file: lab9.py
problem: Use Python's built-in list functions, Develop while control structures, Use boolean logic
certification of authenticity:
I certify that this assignment is entirely my own work.
"""

from graphics import *
from door import Door
from button import Button

def main():
    win = GraphWin("Test", 500, 500)
    win.draw(win)
    button = Button(shape, label)
    door = Door(rectangle, string)
    door.draw(self, win)
    button.draw(self, win)

    door_clicked = door.is_clicked()
    door_name_clicked = door.is_clicked()

    if door_clicked:
        self.shape.setFill()
    else:
        None

    if door_name_clicked:
        label = self.text.getText()
        return label
    else:
        None

    win.getMouse()
    win.close()








if __name__ == '__main__':
    pass









